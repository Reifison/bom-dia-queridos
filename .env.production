# Embutido em `vite build` (App Store / TestFlight / Capacitor).
#
# ATENÇÃO: enquanto isto for um domínio fictício ou inexistente, o iPhone NÃO consegue gerar mensagens.
# Coloca aqui o URL HTTPS público onde o teu servidor Node (pasta server/) está deployed
# (Railway, Render, Fly.io, VPS, etc.) ou um túnel HTTPS temporário (ex.: ngrok) para testes.
#
VITE_API_ORIGIN=https://api-6b3jaanzca-uc.a.run.app
# Opcional: só se o build ainda trouxer localhost (ex.: variável no terminal).
# VITE_API_ORIGIN_PRODUCTION_FALLBACK=https://SUA-API-AQUI.com
