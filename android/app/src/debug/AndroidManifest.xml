<?xml version="1.0" encoding="utf-8"?>
<!-- Local HTTP API access is enabled only for debug builds. Release keeps the
     platform default (cleartext disabled) and must use an HTTPS API origin. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
